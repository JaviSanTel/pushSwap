

0. FLAGS. hay que detectar flag como argumentos, pueden indicar qué estrategia utlizar:

	--simple
	--medium
	--complex
	--adaptive

puede haber flags o no. DECIDIDO si DOS FLAGS ----ERROR---- CONSULTAR.
Entonces primero paso todos los argumentos por strategy_flag (0.1) y ahí si ha encontrado flag, a 
variable "strategy" (un int) le doy el valor de la flag (la inicialicé) si en el siguiente 
argumento vuelve a encontrar flag, llama a error_exit.

	0.1. strategy_flag
		En stategy_flag llamo a un strcmp (el de la libf es strncmp) y le mando cada
		posible flag (--simple, --medium, --complex, --adaptive) que lo compare con 
		cada argumento. Si si, devuelve un número diferente, dependiendo de la flag.

1. Protección si solo hay un argumento. (solo el programa)
	if (argc < 2)  --- return (0);

2. voy a reservar memoria, para cuando lo tenga, los números, guardarlos en algún sitio.
	Como no se cuando miden, lo que voy a hacer son cálculos por arriba, cuento cuántos 
	caracteres tiene cada argumento, y los voy sumando (max_size = max_size + ft_strlen(argv[i])
	 para saber como MÁXIMO cuanto puede ocupar esa lista de números (PROVISIONAL) y reservamos
	con malloc.

3. paso todos los argumentos por el SPLIT (si no hay espacios, pues va a devolver lo que haya, y si hay
	espacios, pues va a separar los "números") no va a romper nada si no hace falta. Pasamos todo y
	listo.

4. a la vez que paso cada argumento por el split, pasa la validación de si es un número 
	(la de is_valid_number) 4.1. y también la de si está en los límites de int, a la vez
	que lo convierte en int (str_to_checked int) 4.2. y lo va guardando en la memoria que hemos 
	reservado en el punto 2. 
	
	4.1. is_valid_number  -- valida si solo hay un signo, si hay más pues error (4.3.) y si son 
		dígitos [0-9] si (!is_valid_number) llamamos a error_exit (4.3.)

	4.2. str_to_checked int -- esta hace dos cosas, vamos es como un atoi un poco trucado, a la 
		vez que convierte a int comprueba si cabe en un int, con INT_MAX Y MIN, que por eso 
		llamamos a la librería "limits". como utilice un long para ver si cabe o no en el int, 
		al hacer el return casteo a int. Si NO cabe en int llamo a error_exit (4.3.)

	4.3. error_exit -- vale, esta es muy sencilla, la he puesto a parte porque así la llamamos desde
		cualquier sitio, simplemente imprime "Error\n" por la salida 2, que es la de errores.
		y utiliza exit para romper, vamos, para el programa.

5. Vale, lo tendriamos guardada esta "lista" , vamos, ahora lo tenemos en un array los datos, que ya si
	sabemos que son números, falta comprobar si hay algún duplicado. OTRA FUNCIÓN, has_duplicates,
	le pasamos el array, NUMBERS, y cuantos elementos tiene el arrays, COUNT.
	 Entonces, que si encuentra duplicidad, pues llamamos a 4.3. y salimos y fin. Que no hay
	 duplicidad, vale, pues entonces tenemos un array con los números guardados.

6. He añadido el archivo psindex que es el que indexa todo el array que has creado y hacer k corra 
	los numeros del 1 a N. 

7. Tambien he creado el archivo disorder que seria la funcion que calcula el indice de desorden en 
	la lista.

8. Por ultimo he hecho un archivo llamado diseño_algoritmo que es lo k dice el nombre XD. Despues de
	dos dias de cabezoneria y papeles gastados ya tengo los algoritmos para el medio y el dificil. 
	El medio es el ksort y el dificil el radix. Ya se perfectamente como implementar los dos y en ese 
	documento lo tengo todo detallado y en pseudocodigo para k lo implementemos en algun momento al codigo.
	Si puedo mañana empezare con ello. 

